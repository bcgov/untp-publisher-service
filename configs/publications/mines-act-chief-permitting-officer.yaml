# One issuer per publication config.
# Templates: configs/templates/{type}.{version}.yaml
# OCA:        configs/oca/{type}.{version}.json
# Samples:    configs/samples/{type}.{version}/publication-payload.json (+ issued-credential.json)

issuer:
  id: did:web:registry.digitaltrust.gov.bc.ca:mines-act:chief-permitting-officer
  name: Chief Permitting Officer
  description: The Chief Permitting Officer is responsible for issuing Mines Act permits.

credentials:
  - type: BCMinesActPermitCredential
    version: v1.1

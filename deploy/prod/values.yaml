## Production overlay — namespace f890b1-prod.
##   cd charts/untp-publisher && helm dependency update
##   helm upgrade --install untp-publisher-service . -f ../../deploy/prod/values.yaml -n f890b1-prod
##
## Public URL: standard Gold Route host (*.apps.gold.devops.gov.bc.ca).
## OpenShift edge termination — no ingress.tls / no custom cert Secret.
##
## Pre-create Traction Secret untp-publisher-service-traction-tenant-info
## (keys: traction_tenant_id, traction_api_key) — see backend.traction.existingSecret.
##
## Image: landing + Discovery UI build (same tag as f890b1-dev until a release tag ships).

ingress:
  annotations:
    route.openshift.io/termination: edge
  # No tls: — router terminates HTTPS.

networkPolicy:
  ingress:
    namespaceSelector:
      network.openshift.io/policy-group: ingress

backend:
  testSuite: false
  image:
    repository: ghcr.io/bcgov/untp-publisher-service
    tag: dev-0.0.4
    pullPolicy: Always
  # Hostname only (sets Ingress rule + pod PUBLISHER_DOMAIN).
  # Public origin: https://untp-publisher-api.apps.gold.devops.gov.bc.ca
  host: untp-publisher-api.apps.gold.devops.gov.bc.ca
  traction:
    apiUrl: "https://traction-tenant-proxy-prod.apps.silver.devops.gov.bc.ca"
    existingSecret: untp-publisher-service-traction-tenant-info
  environment:
    webvhServerUrl: "https://registry.digitaltrust.gov.bc.ca"
    # Witness multikey from former orgbook-publisher prod chart → did:key.
    publisherWitnessId: "did:key:z6MkpJybUKKTCjrYPL6B6EWw534ESR3oEC6Bo2XSAefeUG7Y"
    projectTitle: "BC NRS Publisher"
    projectVersion: "v0"
    landingTagline: "Verifiable critical raw material provenance for British Columbia's Natural Resource Sector."
    landingDescription: "Browse UNTP-aligned credentials that attest critical raw material provenance across the Natural Resource Sector—copy public URLs, download VCs, and verify issuance from this publisher."
    landingLogoUrl: "/static/admin/images/bcgov-logo.svg"
    landingPrimaryColor: "#013366"
    landingSecondaryColor: "#FCBA19"
  resources:
    limits:
      cpu: 300m
      memory: 512Mi
    requests:
      cpu: 50m
      memory: 256Mi

mongodb:
  customUser:
    # Must match Helm release name untp-publisher-service (parent-managed secret).
    existingSecret: untp-publisher-service-mongodb-custom-user-secret
  targetPlatform: openshift
  # Namespace storage quota is 1Gi; MongoDB 8 needs ~500Mi free → use full 1Gi.
  persistence:
    storageClass: netapp-block-standard
    size: 1Gi
  # 512Mi limit previously OOMKilled under mongosh probes; 768Mi + cache cap is stable
  # without raising request quota (still 256Mi request).
  resources:
    limits:
      memory: 768Mi
    requests:
      cpu: 25m
      memory: 256Mi
  config:
    content: |
      systemLog:
        quiet: true
        verbosity: 0
      net:
        bindIpAll: true
      storage:
        wiredTiger:
          engineConfig:
            cacheSizeGB: 0.25
  livenessProbe:
    initialDelaySeconds: 45
    periodSeconds: 30
  readinessProbe:
    initialDelaySeconds: 10
    periodSeconds: 30
  commonLabels:
    backup: "true"
    env: prod
